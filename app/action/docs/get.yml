Get details about an action.
---
tags:
  - actions
parameters:
  - description: Action to get details
    name: id
    required: true
    type: integer
    in: path
responses:
  200:
    description: Success.
    schema:
      "$ref": "#/definitions/Action"
  400:
    description: Action failed
  401:
    description: Unauthorized
  404:
    description: Action does not exist